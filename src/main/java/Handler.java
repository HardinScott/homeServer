public abstract class Handler {
    //this might be better as an interface
    //might want to string together handlers or make an action class that acts as linked list for each action
    public abstract boolean Handle();
}
